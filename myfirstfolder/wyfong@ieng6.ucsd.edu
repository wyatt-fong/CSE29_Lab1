#include <stdio.h>

int main() {
	char hello[] = "This is a new version of hello world for extra practice!";
	char extra[] = "Extra stuff to work with if needed later on";

	puts(hello);
	puts(extra);

	return 0;
}
